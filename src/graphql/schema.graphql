scalar PeerGraphQLDateTime

type PeerGraphQLKeypair {
  pair: String
}

# Used for local state to resolve query only once among peers
type PeerGraphQLResolution {
  hash: String
  time: PeerGraphQLDateTime
}

extend type Query {
  resolution(hash: String): [PeerGraphQLResolution]
}
